<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="XTravelAlarm.Views.MainPage"
             Title="XTravel Alarm">

    <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="20,10,20,10" Orientation="Vertical">

        <Button Text="Pobierz lokalizacje"
                Clicked="GetLocalizationClicked" />
              
        <maps:Map WidthRequest="320" HeightRequest="200"
                  x:Name="MyMap"
                  IsShowingUser="true"
                  MapType="Street" />

        <StackLayout HorizontalOptions="Start" VerticalOptions="Start"  Orientation="Horizontal" Margin="0,15,0,5">
            <Label Text="Latitude:"
                   FontSize="17" />
            <Label Text="{Binding Latitude}"
                   FontSize="17" />
        </StackLayout>
        <StackLayout HorizontalOptions="Start" VerticalOptions="Start" Orientation="Horizontal">
            <Label Text="Longitude:"
                   FontSize="17" />
            <Label Text="{Binding Longitude}"
                   FontSize="17" />
        </StackLayout>


    </StackLayout>
</ContentPage>